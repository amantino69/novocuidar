# TeleCuidar Maleta - Requisitos Python

# BLE (balança, pressão, termômetro)
bleak>=0.21.0

# HTTP (comunicação com servidor)
aiohttp>=3.9.0

# Ausculta - estetoscópio USB
sounddevice>=0.4.6
numpy>=1.24.0

# Serviço Windows (opcional, só se usar pywin32 em vez de nssm)
# pywin32>=306
